var canvas = document.getElementById('canvas'), // Somehow if use jQuery selector, the canvas is scaled -____-
	context = canvas.getContext('2d'),
	WIDTH = $('#canvas').width(),
	HEIGHT = $('#canvas').height();

	console.log(WIDTH, HEIGHT);